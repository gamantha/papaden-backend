import { FileextenderInterceptor } from './fileextender.interceptor';

describe('FileextenderInterceptor', () => {
  it('should be defined', () => {
    expect(new FileextenderInterceptor()).toBeDefined();
  });
});
